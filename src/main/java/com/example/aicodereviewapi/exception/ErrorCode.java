package com.example.aicodereviewapi.exception;

public enum ErrorCode {
    UNKNOWN_ERROR,
    NOT_FOUND,
    VALIDATION_ERROR,
    UNAUTHORIZED,
    FORBIDDEN,
    EMAIL_ALREADY_EXISTS,
    INVALID_CREDENTIALS
}
